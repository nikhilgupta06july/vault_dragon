class Api::VaultDragonsController < Api::BaseController


  def show
    
  end
  
  def index
   

  end

  def create
    if request.body.present?
	 	response_hash = JSON.parse(request.body)
	 	
	 else
	 	render json: "Invalid Request", status: 400 and return
	 end
  end

  def update

  end

  def vault_params
   
    params.require(:item).permit(:item_type, :title, :description, :state_id, :subtitle, :page_count, :country_id,:user_id, :exchanged_at , :location, :latitude, :longitude )
  end

end
